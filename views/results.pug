html
head
	meta(charset='UTF-8')
	title SyncAni
body
	h1 SyncAni
	form(action='/search' method='post')
		input(type='text' name='title' placeholder='Anime Title')
		button.btn search
	h1 Results from you search: #{ search }
	ul
		each val, name in animeResults
			li
				a(href=val + '/episodes') #{ name }